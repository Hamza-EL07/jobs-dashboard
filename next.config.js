/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "9djv4bk1udvpigib.public.blob.vercel-storage.com",
      },
    ],
  },
};
module.exports = nextConfig;
